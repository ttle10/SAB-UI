@using SystemeAideBasculement.Components.Layout.Common
@using SystemeAideBasculement.Models

<div class="panel-header">
    <div class="panel-title">
        Aide mémoire
        <ErrorBanner @ref="_errorBannerCtrl" />
    </div>
    
</div>

@code {

    //private ICustomMessage? _displayException { get; set; }

    private ErrorBanner? _errorBannerCtrl;

    public void SetMessage(ICustomMessage? ex)
    {
        if (_errorBannerCtrl != null)
        {
            _errorBannerCtrl.SetMessage(ex);
        }
        //StateHasChanged();
	}
}